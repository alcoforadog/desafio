---
- hosts: all
  become: true
  roles:
  - basic

- hosts: server1
  become: true
  vars:
    NODEJS_VERSION: "12"
    ansible_distribution_release: "bionic"
    app_path: "/var/www"
    app: "example"
  roles:
  - node
  - example